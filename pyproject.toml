[project]
name = "teachhinglish"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ambuj1211", email = "brilliantambuj5@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "google-genai>=2.20.0",
    "langgraph>=1.2.11",
    "openai>=3.6.0",
    "pydantic-settings>=2.15.0",
    "sympy>=1.14.0",
]

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
]
